import React, { useState } from 'react';
import { Button, Dialog, DialogActions, DialogContent, DialogTitle, Paper, Typography, Grid } from '@mui/material';


import LocalTable from './LocalTable';



const LocalList = () => {
    
    return (
        <>            
            <LocalTable />
       
        </>
    );
};

export default LocalList;